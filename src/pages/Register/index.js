import React, { useState, } from 'react';
import PropTypes from 'prop-types';

const propTypes = {};

function Main() {
	return (
		<div>
			register
		</div>
	);
}

Main.propTypes = propTypes;

export default Main;